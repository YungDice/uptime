node_modules/
dist/
dist-demo/
dist-ssr/
*.local

# Tauri build output and generated platform projects
src-tauri/target/
src-tauri/gen/

# What scripts/stage-release.mjs hands to the release step
release/

# Environment
.env
.env.local
.env.*.local

# Impeccable working files: review captures and build state are per-run
# artifacts. The surface briefs, PRODUCT.md and config are not - those are
# the durable record and belong in the repo.
.impeccable/review/
.impeccable/build/
.impeccable/mocks/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

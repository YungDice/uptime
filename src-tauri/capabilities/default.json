{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Uptime needs to show the check-in prompt and nothing else.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
